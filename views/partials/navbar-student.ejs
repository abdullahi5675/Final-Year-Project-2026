<!-- Student Navbar -->
<nav class="navbar">
    <div class="navbar-brand">
        <img src="/FUTBBBBBBB.png" alt="FUTB Logo">
        <div class="navbar-brand-text">
            <div class="portal-title">NELFUND Refund Portal</div>
            <div class="portal-sub">Federal University of Technology, Babura</div>
        </div>
    </div>
    <!-- Hamburger Toggle -->
    <button class="navbar-toggle" id="navbarToggle" aria-label="Toggle navigation">
        <span class="bar"></span>
        <span class="bar"></span>
        <span class="bar"></span>
    </button>
    <div class="navbar-right" id="navbarRight">
        <span class="user-name"><%= student.full_name %></span>
        <a href="/student/dashboard"<% if (typeof activePage !== 'undefined' && activePage === 'dashboard') { %> class="active-link"<% } %>>Dashboard</a>
        <a href="/student/complaints"<% if (typeof activePage !== 'undefined' && activePage === 'complaints') { %> class="active-link"<% } %>>Complaints</a>
        <a href="/logout" class="btn-logout">Logout</a>
    </div>
</nav>
